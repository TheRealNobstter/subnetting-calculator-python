# subnetting-calculator-python
IPv4 subnetting calculator with a Tkinter GUI (currently in German). Supports both RFC 950 and RFC 1878 and calculates networks, hosts, broadcast addresses, and subnet ranges.
